<h1>Companies</h1>
<ul>
  <% @companies.each do |company| %>
    <li> <%= link_to "#{company.name}", company_path(company) %></li>
  <% end %>
</ul>
<p><%=link_to "Add your company!", new_company_path %></p>
